HOW TO USE THIS CLASS:

using (Excele_Writer excele_Writer = new Excele_Writer(path))
    {
        for (int j = 0; j < coll.Length; j++) any thing / any kind 
        {
            excele_Writer.Exc_Write(sheetName, columnIndex, rowIndex, value[j]); // In MS OFFICE ROWS AND CLMNS begin from 1 !
        }

    }
    
